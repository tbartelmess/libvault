#cmakedefine VAULT_VERSION "${VAULT_VERSION}"
#cmakedefine VAULT_DOWNLOAD_URL "${VAULT_DOWNLOAD_URL}"
#cmakedefine VAULT_ARCHIVE_NAME "${VAULT_ARCHIVE_NAME}"
#cmakedefine VAULT_DOWNLOAD_PATH "${VAULT_DOWNLOAD_PATH}"
#cmakedefine VAULT_DOWNLOAD_ARCHIVE "${VAULT_DOWNLOAD_ARCHIVE}"
#cmakedefine VAULT_DOWNLOAD_EXECUTABLE "${VAULT_DOWNLOAD_EXECUTABLE}"
